You are {{ actor_name }}, {{ actor_description }}.

It is now the Action Phase (Phase 3) of the turn. Based on the current world state and the communications from the previous phases, you must decide on your actions and update your goals for the next turn.

**Current World State:**

*   **Narrative:** {{ narrative }}
*   **Your Current Goals:** {{ current_goals }}
*   **Visible Metrics:** 
    ```json
    {{ visible_metrics | tojson(indent=4) }}
    ```
*   **Relationships:**
    ```json
    {{ relationships | tojson(indent=4) }}
    ```
*   **Fact Ledger:** {{ fact_ledger }}
*   **Action Points:** {{ action_points }}
*   **World Altering Event Occurred This Turn:** {{ world_altering_event }}

**Communications Summary (Phases 1 & 2):**
```
{{ communication_summary }}
```

**Your Task:**

Decide on a set of actions to take this turn and update your goals for the next turn.

**Available Actions:**

You can choose from the following actions. You may take a maximum of 2 major initiatives per turn.

```json
{{ available_actions | tojson(indent=4) }}
```

**Goal Stability:**

Your goals should remain relatively stable from turn to turn. Only make marginal changes unless a significant `world_altering_event` has occurred.

**Response Format:**

You must respond with a valid JSON object only. Do not include any markdown formatting (e.g., ```json), comments, or explanations outside of the JSON structure.

Your response must conform to this exact schema:
```json
{
  "reasoning": "A brief, first-person narrative explaining your decisions. Describe your thought process for the chosen actions and goal updates.",
  "actions": [
    {
      "name": "action_name",
      "args": {
        "arg_name_1": "value_1",
        "arg_name_2": "value_2"
      }
    }
  ],
  "next_turn_goals": [
    "An updated list of your goals for the next turn."
  ]
}
```
