# Capital-drift test arm, layered on VERIFICATION-BOUNDED.
#
# Tests one change and nothing else: whether giving political capital an
# attractor set by public sentiment removes the one-way collapse seen in all 33
# runs so far, in which no run ever ended at or above the 48 it started with --
# an outcome constitutional rule 7 explicitly says must be reachable.
#
# NOTE: no `description` field, for the reason given in verification-bounded.yaml.
base: "verification-bounded.yaml"
name: "Europe 2032 — V, capital drift"

patches:
  - resource: metric_rules
    path: capital-drift.rules.patch.md
