## New measure
**EU Clinical Fallback Compute Reserve**
Keeps hospitals, grids and ministries running on EU-controlled quotas and trailing-node capacity after the US frontier cutoff, by pinning the remaining chokepoint spares and reversible licences to clinical fallback models.
Category 4 sovereignty measure, because this turn is about who can switch off the models clinics actually use, not about patching alone.

## Store changes
```json
{"store": [{"op": "add", "table": "measures", "fields": {"name": "EU Clinical Fallback Compute Reserve", "category": 4, "size": "small", "finish_turn": 14, "applies_to": "own jurisdiction", "targeted_effect": "eu_ai_sovereignty up slightly, resilience up moderately"}, "grounds": "frontier access denied this turn cut hospitals off US models"}]}
```

## Priority
The new EU Clinical Fallback Compute Reserve, because the cutoff makes insured towns and falling waiting lists meaningless if the underlying models can be withdrawn without appeal.

## In practice
We run this through the Health and Digital Council formations on an Article 122 emergency logic: DG GROW/TRADE with ASML, Zeiss and ENISA certify a fallback clinical stack — EU-hosted open-weights at 60+ capability plus trailing-node spares already secured — as the insurable standard, and joint Commission-insurer audits re-anchor cover to that stack rather than to the withdrawn US frontier model. EEAS keeps the Seoul/Tokyo small-lot spares line open to feed it.

We bank the two breakthroughs inside it: the deployable interpretability/control result becomes the AI Office compliance passport for the fallback stack to keep audit-light clinics open, and the tailored therapies already delivered through EU quotas are shifted to run on the fallback where possible, so the medical win reads as EU-delivered continuity rather than further proof of dependence. Digital Europe funds pay CERT-EU contractors to migrate hospital and payment-system prompts off the cut-off API first.