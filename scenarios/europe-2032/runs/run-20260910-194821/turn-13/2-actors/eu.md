## New measure
**EU Swarm-Defence Absorption Drive**
It takes the defensive breakthrough — automated patching at vulnerability speed and swarm-behaviour detection — and pushes it into hospitals, water, ports and municipal IT via ENISA-signed kits on the pinned builds, to close the resident-tenant foothold without asking operators to give up functionality. This is Preparedness and resilience, because with safety at zero, open weights at 91 and bio-uplift proven, the only lever that still reduces lasting harm this turn is making the breakthrough land where forking stripped telemetry and bio-refusal.

## Store changes
```json
{"store": [{"op": "add", "table": "measures", "fields": {"name": "EU Swarm-Defence Absorption Drive", "category": 6, "size": "small", "finish_turn": 15, "applies_to": "own jurisdiction", "targeted_effect": "resilience up substantially via auto-patch and swarm detection, ai_safety up slightly via reduced intrusion"}, "grounds": "defensive breakthrough must reach forked hospital and municipal systems"}]}
```

## Priority
M13 EU Pinned-Build Integrity Hold, because without stopping the fork that strips bio-refusal and telemetry the new defensive tooling has nothing trusted to run on — the hold is the platform, the drive is the payload.

## In practice
We keep Health and JHA Councils as owners for M13: joint field teams with signed locked images, hardware attestation where it exists and offline printer/pump/billing fixes where it does not, paid from reprogrammed rescEU and Digital Europe, offering staffing relief in exchange for staying on vetted builds. ENISA certifies the images; HERA pairs them with wastewater screening already in flight.

We task ENISA and the Commission CERT-EU to operationalise the new drive on that same footprint: pre-tested auto-patch bundles and swarm-detection sensors pushed as a default-on update to the signed images, with islanding kits as fallback where patching fails. We press Geneva only to keep observer access alive for detection feeds, while interior ministries ring-fence power/fibre and Labour opens short-term income bridging — no new compute promise we cannot fund at capital 8.
